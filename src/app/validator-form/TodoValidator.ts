// import { Validators, FormControl, FormGroup, ValidatorFn, AbstractControl, ValidationErrors } from "@angular/forms";

// export class TodoValidator extends Validators  {
//     formGroupTodo: FormGroup = new FormGroup({
//         todo: new FormControl('', [
//             Validators.required,
//             Validators.minLength(3)
//         ])
//     })

//     static invalidedCharacter (control: FormControl) {
//         if (control.value && control.value.length > 0)
//     }
// }